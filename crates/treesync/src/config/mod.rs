//! Reading and validating a configuration file.
//!
//! [`mod@file`] is the whole of it: the TOML shapes an operator writes, and the
//! resolved form the running components take once `[defaults]` have been
//! applied.

pub mod file;
